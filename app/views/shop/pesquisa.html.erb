<% provide(:title, 'Pesquisa') %>
<h1>Pesquisa</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@shop) do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.submit "Pesquisar", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>